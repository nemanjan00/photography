{
  "name": "frames",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A timeline photo gallery — add photos, push, done. Static, SEO-first, zero hand-written CSS.",
  "license": "MIT",
  "author": "Nemanja Nedeljkovic",
  "scripts": {
    "ingest": "node scripts/ingest.mjs",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "npm run build && npm run serve",
    "preview": "npm run build && npm run serve"
  },
  "dependencies": {
    "@fontsource/fira-code": "^5.3.0",
    "@nemanjan00/qrp": "^0.7.0",
    "marked": "^18.0.6",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "exifr": "^7.1.3",
    "piexifjs": "^1.0.6"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "@playwright/browser-chromium@1.61.1": true
  }
}
